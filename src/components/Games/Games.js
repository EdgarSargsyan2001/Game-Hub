import { useNavigate } from "react-router-dom";
import Card from "./HomeGameCard";
function Games() {
   
    return (
        <div>
            < Card/>
        </div>
        );
}

export default Games;
